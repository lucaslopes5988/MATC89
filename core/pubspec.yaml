name: core
description: Core infrastructure for Playce.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  commons:
    path: ../commons
  dio: ^5.8.0+1
  firebase_core: ^3.13.0
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  injectable: ^2.5.0

dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^5.0.0
  injectable_generator: ^2.7.0
