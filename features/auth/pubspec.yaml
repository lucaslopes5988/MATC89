name: auth
description: Authentication feature for Playce.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  commons:
    path: ../../commons
  core:
    path: ../../core
  design_system:
    path: ../../design_system
  equatable: ^2.0.7
  firebase_auth: ^5.5.2
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get_it: ^8.0.3
  google_sign_in: ^6.3.0
  injectable: ^2.5.0

dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.7.0
  mocktail: ^1.0.4
