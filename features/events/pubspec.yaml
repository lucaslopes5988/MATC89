name: events
description: Events feature for Playce.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  cloud_firestore: ^5.6.6
  firebase_core: ^3.13.0
  commons:
    path: ../../commons
  core:
    path: ../../core
  design_system:
    path: ../../design_system
  equatable: ^2.0.7
  firebase_auth: ^5.5.2
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  geoflutterfire_plus: ^0.0.28
  geolocator: ^13.0.2
  get_it: ^8.0.3
  google_maps_flutter: ^2.12.1
  injectable: ^2.5.0
  intl: ^0.20.2
  permission_handler: ^11.3.1
  url_launcher: ^6.3.2
  geocoding: ^4.0.0

dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.7.0
  mocktail: ^1.0.4
